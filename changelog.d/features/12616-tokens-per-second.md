- **feat(api): emit generation tok/s and TTFT on inference responses** — non-stream success headers now include `X-OmniRoute-Tokens-Per-Second` (output tokens / generation seconds; TTFT excluded when known) and optional `X-OmniRoute-Ttft-Ms`. The same values land on `usage.tokens_per_second` / `usage.ttft_ms` so SDKs that drop custom headers still see them. Streaming final SSE metadata comments carry the same fields. `X-OmniRoute-Latency-Ms` remains end-to-end. (#12616)
